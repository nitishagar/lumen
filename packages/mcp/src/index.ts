/**
 * @seolite/mcp — placeholder barrel (M0 scaffold, Phase 1).
 * Real API (MCP tool definitions + transport adapters, Cloudflare Worker)
 * lands in the surfaces aspect (P4).
 */
export const packageName = '@seolite/mcp';
