/**
 * @seolite/providers — placeholder barrel (M0 scaffold, Phase 1).
 * Real API (built-in providers, BYOK-absent skip semantics, per-provider
 * attribution) lands in the providers aspect (P3).
 */
export const packageName = '@seolite/providers';
