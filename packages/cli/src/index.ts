/**
 * @lumen-seo/cli — placeholder barrel (M0 scaffold, Phase 1).
 * Real API (CLI commands, JSONL HistoryStore, stdio MCP launcher) lands in
 * the surfaces aspect (P4).
 */
export const packageName = '@lumen-seo/cli';
