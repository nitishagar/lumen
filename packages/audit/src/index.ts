/**
 * @lumen-seo/audit — placeholder barrel (M0 scaffold, Phase 1).
 * Real API (bounded crawler, built-in AuditRules, severity scorer, report
 * assembly with output-sanitization ownership) lands in the audit-engine
 * aspect (P2).
 */
export const packageName = '@lumen-seo/audit';
