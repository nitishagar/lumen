# lumen — State Ledger (2026-08-29 ~21:40 IST)

Durable snapshot for resumption after context loss / agent outage. Authoritative naming: RENAMES.md (`thoughts/shared/plans/2026-08-29-seolite/RENAMES.md`).

## 1. Objective (user requirements → status)

| # | Requirement | Status |
|---|---|---|
| 1 | Lightweight pluggable Semrush alternative, public Apache repo on gh user `nitishagar` | **DONE** repo live: https://github.com/nitishagar/lumen (renamed from seolite; Apache-2.0 LICENSE at root, merged via PR #10) |
| 2 | Docs + GitHub Pages site, look/feel like pi.dev | **IN PROGRESS** — site worktree feat/lumen-site ~Phases 1–5 committed, Phase 6 (gates consolidation/hardening) was in flight when agent died; NOT built/verified end-to-end yet; Pages NOT enabled yet |
| 3 | Developer + PM friendly; agent & MCP-first | **IN PROGRESS** — core+providers merged/ready; CLI+MCP surfaces ~60% (Phases 1–3 committed, Phase 4 MCP files untracked WIP, Phases 5–6 Worker/hardening not started) |
| 4 | Red-team verification each stage | Research audited (round 1 fixed); plans adversarially validated round 2 ALL PASS; P6a CI red/green probes done; **M1 per-aspect adversarial code reviews BLOCKED** (agent usage limit until 23:28:28 IST) — must run post-reset before final merge sign-off |
| 5 | TDD implementation, local verification | **DONE for core (179) + providers (119, suite 297/297)**; audit/surfaces/site mid-TDD (see §3) |
| 6 | All-free services (Pages/Actions/Workers) | Architecture enforces; Workers deploy workflows (P6b) NOT written yet; no CF credentials on machine → CI-gated deploy design stands |
| 7 | Vision-model e2e check (app run + pi.dev theme match) | **PENDING** — needs site built + deployed; plan: screenshot CLI + Pages site, native vision review + committed screenshots under docs/screenshots for remote vision model |
| 8 | No co-author attribution anywhere | Enforced by CI `identity` job (author-strict, trailer ban; red-probe verified); all commits to date clean |
| 9 | Product renamed to catchy name | **DONE** — `lumen` (bin `lumen`, scope `@lumen-seo/*`, tagline "See your site clearly."); unscoped npm `lumen` is TAKEN — publish only `@lumen-seo/*` (fallback: `lumen-*` unscoped) |

## 2. Pipeline stages

| Stage | Artifact | Status |
|---|---|---|
| Research | `thoughts/shared/research/2026-08-29-seolite-greenfield-research.md` | DONE + audit round 1 applied (invariants I1–I17) |
| Plans | 6 bundles under `thoughts/shared/plans/2026-08-29-seolite-*` | DONE — adversarial validation round 2: ALL `VERDICT: PASS` (committed 4f11e5c) |
| Contracts | `…/2026-08-29-seolite/{ARCHITECTURE,RECONCILIATION,RENAMES}.md` | DONE — RENAMES (lumen) is the newest authority |
| M0 | scaffold + CI (P6a) + core | **DONE on main**: abea..61aaa05; CI 5 required checks + branch protection live; core 179/179 |
| M1 | audit/providers/surfaces/site in worktrees | providers COMPLETE+pushed; audit/surfaces/site MID-FLIGHT (§3) |
| M2 | integration gate + P6b workflows + release | NOT STARTED |
| Deploy | Pages enable /lumen/ + Worker CI-gated | NOT STARTED |

## 3. Worktree inventory (all based on main 61aaa05)

### .wt/audit — branch `feat/lumen-audit` — LOCAL ONLY (not pushed)
Committed (4): 58dad05 crawl engine (frontier/budgets/worker pool/stop reasons) · 9466383 robots gate + per-host rate limit + sitemap discovery · a66577a redirect classification + body caps + per-URL failure isolation · 547696a AbortSignal cancellation + partial-failure labeling.
WIP uncommitted: modified `src/crawl/{abort,crawler}.test.ts`, `crawler.ts`, `run.ts`; **untracked `src/rules/` (Phase 5: 18 built-in rules — NOT STARTED-to-early) + `src/testing/page.ts`**.
Remaining: Phase 5 rules + severityOverrides, Phase 6 scorer/report assembly/I13 sanitization + `npm test -w @lumen-seo/audit` gate, push, REASONING.md (bundle dir has none yet in this worktree).

### .wt/providers — branch `feat/lumen-providers` — **COMPLETE, PUSHED** (origin/feat/lumen-providers, 9 commits, clean tree)
119/119 package tests (11 files); whole suite 297/297 (27 files); lint+typecheck green. All 60 plan criteria ticked; REASONING.md written.
Deviations (recorded): additive core deltas (A9: `PageSpeedReport.field`, ISO `retrievedAt`); BYOK via headers not `?key=`; worker-safe factory at `@lumen-seo/providers/worker` subpath (NOT barrel — cheerio isolation); npm dep `*` not `workspace:*`.
**NEXT ACTION: open PR + merge (P3 in merge order).** Adversarial code review deferred to post-limit final pass (recorded deviation).

### .wt/surfaces — branch `feat/lumen-surfaces` — LOCAL ONLY
Committed (3): 05447fc CLI skeleton (bin `lumen`, strict args, exit envelope 0/1/2, help pre-intercept, term sanitizer, `config show`) · 7da381d JSONL history store + rank/keywords/authority over fixtures · 407a6d4 audit+report commands via `ports.ts` seam + mcp ports/url-guard.
WIP untracked: `packages/mcp/src/{local-only,onboard,schemas,server,strict-args}.ts` + `src/testkit/` — **Phase 4 (MCP factory/stdio/onboarding) was being written when agent died — files exist but likely incomplete/uncommitted-untested.**
Remaining: finish Phase 4, Phase 5 Worker (createMcpHandler /mcp + REST /healthz,/api/v1/page-report,/api/v1/keyword-ideas; 2.5MB capping fetcher; Miniflare tests; 1.5MB gzip check; bundle-scan), Phase 6 hardening (SIGINT unify, concurrency 25x, CORS, I16 fetch-ban lint), gates, push.
Rebase seam left per plan: real `@lumen-seo/audit`+`providers` wiring happens at orchestrator merge (mcpComposition/ports).

### .wt/site — branch `feat/lumen-site` — LOCAL ONLY
Committed (5): 943978b Astro scaffold + build contract (`npm run build -w @lumen-seo/site` → site/dist/, base /lumen/) · f6f2419 tokens/theming/chrome (G1–G5: token conformance, contrast math, trade-dress scan, escaping ban, theme/reduced-motion) · 9925604 landing (install tabs npm/npx/claude-mcp-add, original SVG figs) · 3284205 docs pages w/ locked-name content (G6/G9) · b906085 Pagefind ⌘K search (G8).
WIP uncommitted: modified Base.astro, 404, docs/mcp-onboarding, index, a11y.test.ts; untracked `docs/` (screenshots?), `site/tests/meta.test.ts` (unique titles/descriptions gate), REASONING.md.
Remaining: Phase 6 gates consolidation (axe over all built pages, internal link/anchor checker, meta gate), clean-checkout `npm run check -w @lumen-seo/site` green, screenshots under docs/screenshots, push.

## 4. Repository / CI state

- main = 61aaa05 (core complete). Protection: required checks `identity, workflow-lint, lint, typecheck, test`; strict; PRs; squash; enforce_admins; linear history; delete-branch-on-merge.
- CI scripts: `scripts/ci/check-commits.mjs` (author-strict Nitish <1592163+nitishagar@users.noreply.github.com>; committer two-tier incl. web-flow squash; dependabot pairing; trailer ban; zero-SHA guard), `scripts/ci/select-workspaces.mjs` (scope=… / ALL).
- Root scripts: lint/typecheck/test/validate. Workspaces: packages/{core,audit,providers,cli,mcp} + site = `@lumen-seo/*`.
- Open dependabot PRs #2–#7 (actions v7, eslint 10, @types/node 26, TS 7.0.2): deliberately DEFERRED to M2 decision (TS 7 may break typescript-eslint 8.x — test before merging; safest: close TS/eslint majors, keep actions bumps if green).
- PRs merged so far: #1 ci/bootstrap, #8 docs, #9 RENAMES, #10 scope rename, #11 core.
- Remote: https://github.com/nitishagar/lumen (gh active account nitishagar; git over HTTPS token).

## 5. Blockers & constraints

1. **Agent-spawn usage limit until 23:28:28 IST 2026-08-29** — killed audit/surfaces/site implementers and blocks adversarial review agents. Work continues SOLO (main context) in worktrees; reviews re-run post-reset before final sign-off.
2. npm unscoped `lumen` TAKEN → publish plan `@lumen-seo/*` only (org creation unverified — verify at publish; fallback `lumen-*`).
3. No Cloudflare credentials locally → deploy-worker.yml must skip-cleanly without secrets (design already validated); actual Worker deploy only when user adds `CLOUDFLARE_API_TOKEN` secret.
4. Pages not yet enabled: at M2 run `gh api -X POST repos/nitishagar/lumen/pages -f build_type=workflow` (404-probe first), after pages.yml merges.

## 6. Exact resumption sequence (post-reset or solo, in order)

1. Merge providers: PR from origin/feat/lumen-providers → checks green → squash-merge (P3). Watch core-delta conflicts: none expected (audit not yet merged; providers' core deltas are additive).
2. Finish audit worktree Phase 5–6 solo (rules 18 + scorer/report/I13 + gates) → push → merge (P2 order technically says audit first, but providers-first is conflict-free since audit touches no provider files; record order deviation).
3. Finish surfaces Phase 4–6 (WIP files exist) → rebase onto main-with-audit+providers → wire real registry/audit-runner at the ports seam (rebase commit) → push → merge.
4. Finish site Phase 6 gates → push → merge.
5. M2 on main: full suite; `npm run validate`; CLI smoke (bin `lumen audit` against local fixture http server); MCP stdio handshake smoke; site build+check.
6. P6b: pages.yml + deploy-worker.yml + release.yml + cli-smoke.mjs per ci-deploy plan Phases 4–7 (identity: squash committer semantics already handled).
7. Enable Pages (`gh api … -f build_type=workflow`), confirm https://nitishagar.github.io/lumen/ live.
8. Vision e2e: screenshots (CLI `--help`/audit output; site landing+docs light/dark) → native vision review vs pi.dev token vocabulary (bg #09090b, text #fafafa, accent #f97316, system stacks, 18px root, radii 8/6/4, mono labels) + commit under docs/screenshots; fix deltas; re-shoot.
9. Final red-team pass (agents post-reset): per-aspect adversarial reviews of merged diffs + security-reviewer on final tree.
10. Decide dependabot PRs; tag v0.1.0; release workflow (npm publish gated on NODE_AUTH_TOKEN — likely absent → release GH-only, record).
